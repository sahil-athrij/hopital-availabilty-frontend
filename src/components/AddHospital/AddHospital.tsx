import React from "react";
import {AuthComponent, AuthProps, AuthState} from "../../api/auth";

export class AddHospital extends AuthComponent<AuthProps, AuthState> {

    render() {
        return <div>
            hello
        </div>
    }
}