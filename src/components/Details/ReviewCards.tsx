import React, { Component } from 'react'
import {ReviewObject} from '../../api/model/model.tsx'

interface ReviewObject{
    model: ReviewObject;
}

export default class ReviewCards extends Component {

    render() {
        return (
            <div>
                
            </div>
        )
    }
}
